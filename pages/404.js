//This is how to add a custom 404 page. It must be named exactly like this for NEXTJS to recognize it.

function NotFoundPage() {
  return (
    <div>
      <h1>Page not found!</h1>
    </div>
  )
}

export default NotFoundPage;