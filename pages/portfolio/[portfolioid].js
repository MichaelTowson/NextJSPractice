import { useRouter } from 'next/router' //special next hook that lets us grab the value in our filename

function PortfolioProjectPage() {
  const router = useRouter();

  return <div>
    <h1>This is the Portfolio Page for </h1>
  </div>
}